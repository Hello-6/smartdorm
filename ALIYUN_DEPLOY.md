# SmartDorm 一键部署到阿里云 Windows Server 指南

---

## 第一步：在阿里云控制台找到你的服务器

1. 打开 https://ecs.console.aliyun.com/
2. 登录你的阿里云账号
3. 你会看到你的 ECS 实例列表，找到以下信息：

```
实例名称：xxxxx
公网IP：   123.56.xx.xx     ← 记下这个 IP
状态：     运行中
```

4. 点击实例名称进入详情页
5. 左侧找到 **"本实例安全组"** → 点击 **"配置规则"**
6. 点击 **"手动添加"**，添加两条规则：

| 规则方向 | 协议类型 | 端口范围 | 授权对象 | 描述 |
|----------|----------|----------|----------|------|
| 入方向 | TCP | 3389 | 0.0.0.0/0 | 远程桌面（系统已有） |
| 入方向 | TCP | 3001 | 0.0.0.0/0 | SmartDorm 网页访问 |

> **注意：** 3389 端口默认应该已经有了，确认一下即可。3001 需要手动添加。

---

## 第二步：重置密码（首次使用）

1. 在 ECS 实例列表页，勾选你的服务器
2. 点击下方 **"重置实例密码"**
3. 设置一个新密码（记下来！）
4. 点击 **"重启实例"**（密码重置后需要重启才能生效）
5. 等待 1-2 分钟重启完成

---

## 第三步：用远程桌面连接服务器

**Windows 电脑自带远程桌面工具：**

1. 按键盘 `Win + R`，输入 `mstsc`，回车
2. 在"计算机"输入你的 **公网 IP**（如 123.56.xx.xx）
3. 点击 **"连接"**
4. 输入用户名：`Administrator`
5. 输入刚才重置的密码
6. 点击确定，等待连接成功

> **第一次连接可能出现证书提示**，点击"是"继续即可。

连接成功后，你会看到 Windows Server 的桌面，就像操作自己电脑一样！

---

## 第四步：上传项目到服务器

连接上远程桌面后，在**服务器里**操作：

### 方法一：直接拖拽文件（推荐）

1. 在本地电脑上，把整个 SmartDorm 项目文件夹**压缩成 zip**
2. 在远程桌面中，打开浏览器，登录微信/QQ/阿里云盘等，把 zip 文件传上去下载
3. 或者更简单：在服务器中打开浏览器，访问 https://smartdorm.loca.lt 下载代码（但需要之前打包）

### 方法二：本地复制粘贴

1. 在本地电脑打开项目文件夹
2. 在远程桌面中打开一个文件夹
3. 复制粘贴（远程桌面支持剪贴板共享）

---

## 第五步：安装 Node.js（在服务器中）

在远程桌面的服务器里操作：

1. 打开浏览器，访问 https://nodejs.org
2. 下载 **Windows 版 LTS**（左侧按钮）
3. 双击安装包，一路点"Next"
4. 安装完成后，按 `Win + R`，输入 `cmd`，回车
5. 输入以下命令验证安装成功：

```cmd
node --version
npm --version
```

应该显示：
```
v22.x.x
10.x.x
```

---

## 第六步：运行一键部署脚本

我已经写好了部署脚本，你需要在服务器里创建这个文件。

> **在远程桌面的服务器里**，打开记事本，复制以下内容：

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo    SmartDorm 一键部署脚本
echo ========================================
echo.

cd /d C:\SmartDorm

echo [1/4] 安装后端依赖...
cd api
call npm install
if %errorlevel% neq 0 (
    echo 后端依赖安装失败，请检查网络
    pause
    exit /b
)

echo [2/4] 构建前端...
cd ..
call npm install
call npm run build
if %errorlevel% neq 0 (
    echo 前端构建失败
    pause
    exit /b
)

echo [3/4] 配置防火墙...
netsh advfirewall firewall add rule name="SmartDorm" dir=in action=allow protocol=TCP localport=3001 >nul 2>&1

echo [4/4] 启动服务...
cd api
start "SmartDorm" cmd /c "npm run dev"

echo.
echo ========================================
echo   部署成功！
echo   访问地址: http://localhost:3001
echo   公网地址: http://你的公网IP:3001
echo ========================================
echo.
pause
```

保存文件名为：`C:\deploy.bat`（保存类型选"所有文件"）

然后双击运行 `C:\deploy.bat`。

---

## 第七步：访问你的系统

部署成功后，在**任何设备**的浏览器中打开：

```
http://你的公网IP:3001
```

例如：`http://123.56.xx.xx:3001`

### 测试账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| adm | 123456 | 管理员 |
| 张三 | 123456 | 普通用户 |

---

## 第八步（可选）：让系统在后台持续运行

关闭远程桌面后，系统可能停止运行。用以下方法让它持续运行：

### 方法：使用 PM2（进程管理器）

在服务器中打开命令提示符：

```cmd
npm install -g pm2
cd C:\SmartDorm\api
pm2 start src/index.ts --name smartdorm --interpreter tsx
pm2 save
```

这样关闭远程桌面后，系统依然在线！

---

## 常见问题

### Q1: 浏览器访问提示"无法访问此网站"

**原因：** 安全组规则没配好。
**解决：** 回到第一步，确认安全组中 3001 端口已添加，授权对象为 `0.0.0.0/0`。

### Q2: 部署脚本运行时 npm 下载慢

**解决：** 在服务器中运行以下命令切换镜像源：

```cmd
npm config set registry https://registry.npmmirror.com
```

然后再重新运行部署脚本。

### Q3: 远程桌面连不上

**原因：** 
- 服务器未开机
- 安全组 3389 端口未放行
- 密码未重置

**解决：** 在阿里云控制台确认实例状态为"运行中"，检查安全组规则。

### Q4: 关掉远程桌面后网站打不开了

**解决：** 按第八步安装 PM2，或者保持远程桌面最小化不要关闭。

---
